server:
  port: 8085
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/pablo-davila1/Microservices-tut
          search-paths: config-data
  application:
    name: config-service
